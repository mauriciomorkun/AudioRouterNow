# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual Environments
.venv/
venv/
env/

# PyInstaller
installer/build_output/
installer/dist/
installer/*.spec.bak
legacy-v3/installer/build_output/
legacy-v3/installer/dist/
legacy-v3/installer/*.spec.bak

# DMG Build Artefakte
installer/dmg_background.png
legacy-v3/installer/dmg_background.png
/tmp/*_dmg_staging/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
*.swp
*.swo

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# App Config (user-spezifisch, kein Repo-Inhalt)
engine/.audiorouter/

# Xcode Build
*.xcworkspace
xcuserdata/
DerivedData/

# Compiled Python
*.pyc

# Projektinterne Dokumente die nicht ins Public-Repo gehören
*.pdf
AudioRouterNow_Roadmap_2026.pdf
AudioRouterNow_Strategie_2026.pdf

# Strategie & interne Planung — privat (NIEMALS in GitHub)
LAUNCH_PLAN.md
LAUNCH_EXECUTION.md
AUDIT_REPORT.md
BACKLOG.md
PLAN.md
projekt.md
MARKETING.md
MARKETING_*.md

# Marketing-Automation (lokale Scripts + Kalender — nie ins Repo)
buffer_scheduler.py
content_calendar.json

# Claude-Konfiguration (interne Anweisungen)
.claude/

# Screenshots / Bildschirmfotos im Projektroot
Bildschirmfoto*.png

# Kompilierte Binaries (werden per make gebaut, gehören nicht ins Repo)
helper/AudioRouterNowHelper
legacy-v3/helper/AudioRouterNowHelper

# Hilfsskripte
installer/generate_pdfs.py
legacy-v3/installer/generate_pdfs.py
collect_weekly.py

# Insights-Tracker (Excel liegt in iCloud, Script bleibt lokal)
tracking/

# Feedback & Brainstorming (CASE-*, BRAINSTORM-* — lokal, nicht ins Repo)
feedback/

# Export-Ordner: Grosse Binaries ausschliessen (HTML-Quellen werden committed)
export/*.gif
export/*.zip
legacy-v3/export/*.gif
legacy-v3/export/*.zip
AudioRouterNow-Animation/
AudioRouterNow\ App-Animation.zip

# Dashboard — interne Strategie-Doku & Code (privates Online-Dashboard)
ARN_DASHBOARD_CONCEPT.md
dashboard/

# Diagnostic Reports (werden auf dem Desktop erstellt, nicht im Repo)
AudioRouterNow_DiagReport_*.txt

# Sparkle EdDSA private key — NIEMALS committen
*private_key*
*_BACKUP.txt
sparkle_priv*
*.pem

# Sparkle test app (nicht nötig im Repo)
vendor/Sparkle/Sparkle\ Test\ App.app/
vendor/Sparkle/Symbols/
legacy-v3/vendor/Sparkle/Sparkle\ Test\ App.app/
legacy-v3/vendor/Sparkle/Symbols/
.env

# v4 Swift build artifacts
v4/AudioRouterKit/.build/
v4/**/.build/

# v4 Signing — private Team ID (never commit)
*.local.xcconfig
Signing.local.xcconfig

# v4-appstore/ — private local staging dir (superseded by v4/ in the repo).
# Contains IMPLEMENTATION_PLAN*.md and other private planning docs — never commit.
v4-appstore/

# Persönliche To-Do-Liste (lokal, kein Repo-Inhalt)
TODO.md
