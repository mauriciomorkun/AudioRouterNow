name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  swift-test:
    name: swift-test
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode (Swift 6 / swift-testing)
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.4'

      - name: Run AudioRouterKit tests
        run: swift test --package-path AudioRouterNow4/AudioRouterKit

  xcode-build-test:
    name: xcode-build-test
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode (Swift 6 / swift-testing)
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.4'

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate
        working-directory: AudioRouterNow4

      - name: Build and test app target
        run: >
          xcodebuild
          -project AudioRouterNow4.xcodeproj
          -scheme AudioRouterNow4
          build test
          CODE_SIGNING_ALLOWED=NO
        working-directory: AudioRouterNow4
